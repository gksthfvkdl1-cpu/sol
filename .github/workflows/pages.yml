# GitHub Pages: https://gksthfvkdl1-cpu.github.io/sol/
# 프론트만 배포됩니다. VITE_API_URL 은 아래 build env 에 백엔드 origin 을 둡니다.
# (공개 URL만 저장; DATABASE_URL·JWT 등 비밀값은 저장소에 넣지 마세요.)

name: Deploy to GitHub Pages

on:
  push:
    branches: [main, master]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: package-lock.json
      - name: Install and build
        env:
          VITE_API_URL: https://seven-gir4.onrender.com
          VITE_BASE_PATH: ${{ vars.VITE_BASE_PATH || '/sol/' }}
        run: |
          npm ci
          npm run build
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - id: deploy
        uses: actions/deploy-pages@v4
